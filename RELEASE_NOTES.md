## SolarForge v0.3.21

### 🔧 「⬇ ダウンロードして更新」ボタンが押しても無反応だった問題を修正（重要）

v0.3.14以降、更新ボタンはクリックしても**何も起きない状態**でした（ボタンを組み立てる際の
引用符の扱いに誤りがあり、クリック処理が壊れていました）。本バージョンで根本修正し、
実際にボタンをクリックする自動テストで確認済みです。

> **⚠ このバージョンへの乗り換えだけは手動インストールが必要です**
> （インストール済みの旧バージョンのボタン自体が壊れているため）。
> 本rbzを「拡張機能マネージャ → 拡張機能をインストール」で上書き→SketchUp再起動。
> **次回以降はボタンで完結します。**

### 🔓 社内フィルタの同意が完全自動になりました

同意ページは開いた時点で同意が成立し画面には何も表示されないため、**確認ウィンドウは
もう開きません**。初回のみ「以後自動で同意するか」を画面内ダイアログで確認し、以後は
AI判定・ニュース・機種DB共有・更新ダウンロードのどこで同意が求められても
**バックグラウンドで自動同意→処理を自動再開**します。
手動で確認したい場合は メンテタブ → 🤖AI設定 → 「🔓 確認ページを手動で開く」。

### 🧊 初回読み込み時にSketchUpごとフリーズする問題を修正

原因は同意確認ウィンドウを内部処理の途中で開いていたことでした。ウィンドウ廃止により
根治。あわせてデマンド解析中は「⏳解析中…」を先に表示し、無言で固まって見えないように
しました。

### 🛡 セキュリティ修正

共有DB由来の機種ID等を画面のボタンに埋め込む際のエスケープ不備（理論上のスクリプト
注入経路）を実測で確認し、専用エスケープへ全箇所置換しました。

### 導入

本rbzを拡張機能マネージャで上書き→SketchUp再起動（上記の理由で今回は手動のみ）。
