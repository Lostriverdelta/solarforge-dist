## SolarForge v0.3.24

### 🔧 シートを切り替えると取り込めなくなる問題を修正（重要）

日付列が2本ある電力会社形式（「対象年月」と「対象年月日」が並ぶ・日付列が右の方にある）で、
月の列を日付列と誤認し**365日分が12日に潰れる**不具合を修正しました。
日付列は「一意な日数が最も多い列」を選ぶようになり、
この形式のファイルは**どのシートでもそのまま取り込めます**（AI設計に頼る必要もありません）。

### 🧹 デマンド「② 読み方の確認」のボタンを整理

- **取込ボタンは1本だけ**になりました。AI設計が有効なときは「▶ AI設計で取り込む」に
  自動で切り替わり、手動設定を触ると通常の「▶ この読み方で取り込む」に戻ります
- AI系ボタンも「🤖 AIに再判定させる」の**1本に統合**。通常の判定で決められない書式は
  自動で特殊書式の設計（転置・倍率列など）に切り替わります

### 📝 「365日に足りません（0として扱われ過小）」の表示を修正

v0.3.22から欠落は自動補完されるため、この警告は誤りでした。
「足りない日は取込時に自動補完されます」の案内に差し替え、
実測日数がごく少ない場合のみ推定比重の注意を出します。

### 導入

プラグイン内「⬆更新」ボタンから自動更新できます（v0.3.21以降）。
