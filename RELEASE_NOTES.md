## SolarForge v0.3.14

### 社内ネットワークでも「⬇ ダウンロードして更新」がそのまま使えるようになりました
これまで社内ネットワーク環境では、更新の**確認**はできても**適用**ができず、「配布ページを開く → 手動でダウンロード → 拡張機能マネージャでインストール」という手作業が必要でした。

- **原因**：更新ファイルのダウンロードがプラグイン内部（Ruby）の通信で行われており、社内プロキシや独自の証明書がある環境では**この経路だけが遮断される**ためです。
- **対応**：ダウンロードを**プラグインの画面（ブラウザ）側**で行い、受け取ったファイルを内部へ受け渡してインストールするようにしました。**ボタン1つで更新が完了**します（完了後、SketchUpの再起動で反映）。
- 従来の「📥 手動でダウンロード」ボタンも併置していますので、必要に応じてお使いいただけます。

**安全のための検証**：更新はプログラムをインストールする操作のため、ダウンロードしたファイルが**正規のものと一致するか（SHA-256）とサイズを必ず照合**します。少しでも不一致があればインストールせずファイルを削除し、手動ダウンロードをご案内します。照合情報が取得できない場合も自動更新は行いません。

### 導入
本rbzをSketchUp拡張機能マネージャで上書きインストール→SketchUp再起動、またはプラグイン内「⬆更新」から。
※本バージョン以降、社内ネットワーク環境でも「⬆更新」から自動更新できます。
