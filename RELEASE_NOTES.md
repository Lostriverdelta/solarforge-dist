## SolarForge v0.3.20

### 🤖 デマンドの「AI読み」が標準になりました

デマンドデータを読み込むと、**自動でAIが読み方を判定し、シート・データ範囲・単位を画面に反映**します
（根拠も表示。取込の最終確定は従来どおり「▶ この読み方で取り込む」で人が行います）。
手動で選び直す場合は「⚙ 手動で読み方を設定」を開いてください（AI未設定時は自動で開きます）。

### 🔓 社内フィルタの同意が1操作になりました

AIやニュースが社内URLフィルタの「同意確認」で止まった場合、**確認ウィンドウが自動で開き、
閉じると保留中の処理を自動で再実行**します。従来の「ボタン→🔓→閉じる→もう一度ボタン」の
手間がなくなりました。※同意直後に真っ白なページが出るのは正常です（遷移先がデータ用のため）。

### 📰 ニュースが表示されない問題を修正（社内ネットワーク）

起動時に社内フィルタで遮られると、その後一度も再取得しないままでした。
**同意ウィンドウを閉じた時と、AI設定の「🔌 接続テスト」成功時に自動で取り直します**
（制度・市場ニュースと機種DB共有の両方）。

### 📊 取り込んだデマンドデータをその場で確認できます

取込後に、突合結果の下へ次の3つが表示されます。

- **月別集計表** — 月ごとの需要kWhと最大kW
- **年間ヒートマップ** — 365日×24時間（濃いほど需要大）。営業時間帯・休業日が一目で分かります
- **🔢 数値一覧** — 365日×24時間の全数値（クリックで展開）

ヒートマップの形が実態と合っていなければ読み方の選択が誤っている、という確認がその場でできます。

### 導入

プラグイン内「⬆更新」ボタンから自動更新できます（v0.3.19で修正済みの新しい更新ボタンです。
進捗とログが表示されます）。または本rbzを拡張機能マネージャで上書き→SketchUp再起動。
