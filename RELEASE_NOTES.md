## SolarForge v0.3.9

### AI解説機能（任意・初期は無効）
設計チェックの結果を**AIが日本語で解説**する機能を追加しました。**既定では無効（休眠）で、設定しなければ従来と全く同じ動作**です。

- **最初の機能**：「⚡インバータ整合チェック」の結果に「🤖 この結果をAIで解説」ボタンが出ます（AI設定時のみ）。過電圧・MPPT電圧窓・過積載率について、**なぜ危険か・どう直すか**を実務者向けに説明します。
- **使い方**：メンテ／診断タブの「🤖 AI設定（任意）」に、自社で用意したAI接続先のURLを入れて「接続テスト」。未設定ならボタン自体が出ません。
- **社内ネットワーク対応**：通信はプラグインの画面（ブラウザ）側から行うため、Windowsのプロキシ設定・証明書を利用します。v0.3.8 で確認したとおり、プラグイン内部（Ruby）の通信が遮断される環境でもこの経路は通ります。
- **セキュリティ**：APIキーはプラグインに含めません（配布物からは取り出せません）。キーは自社サーバ側でのみ保持する構成です。接続先が未設定・通信不可の場合は、静かに機能を出さないだけで設計作業には影響しません。

※本機能のご利用には、別途 AI 接続先（自社バックエンド）の準備が必要です。準備がない場合も、このバージョンをそのままお使いいただけます。

### 導入
本rbzをSketchUp拡張機能マネージャで上書きインストール→SketchUp再起動、またはプラグイン内「⬆更新」から。
