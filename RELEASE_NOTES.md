## SolarForge v0.3.13

### 機種データベースの社内共有（新機能・任意）
ご自身で追加した機種（パネル／パワコン／蓄電池）を、**社内の他のPCと共通化**できるようになりました。これまでは各PCごとに手入力し直す必要がありました。

**使い方**
1. DBタブで機種を追加 → **「🔩 共有に申請」**（申請対象は、その端末で追加した機種のみ。同梱機種は対象外）
2. 弊社の承認後、**同じ会社のPCが起動時に自動で取り込み**ます

**安全のための設計**
- 機種の数値（最大DC入力電圧・MPPT電圧範囲など）は**過電圧の判定に直結**します。そのため申請内容は**承認されるまで誰にも配信されません**。申請時・承認時の両方で、電気的な矛盾（MPPT電圧範囲の上限が最大DC入力電圧を超えている等）を自動検査します。
- 取り込みは**追加のみ**です。**すでにお使いの機種の値は上書きされません**（各PCで調整された値をそのまま保持します）。
- 型番の大文字小文字・ハイフン・空白の違いは同一機種とみなすため、**表記違いによる重複登録は起きません**。
- 共有範囲は**会社ごとに分離**されています。他社の機種データが届くことはありません。メーカー正式品として弊社が確認したものは、公式データとして全ユーザーへ配信されます。

**ご利用条件**：AI設定（メンテ／診断タブ）に接続先が設定済みで、ライセンス認証済みであること。未設定の場合はこの機能は動作せず、**従来どおりのローカル動作**になります。

### 導入
本rbzをSketchUp拡張機能マネージャで上書きインストール→SketchUp再起動、またはプラグイン内「⬆更新」から。
