## SolarForge v0.3.19

### ⚠ v0.3.18 以前からの更新は、今回だけ手動で行ってください

本バージョンで修正した不具合（下記①）の影響で、**旧バージョンの「⬇ ダウンロードして更新」ボタンは押しても反応しません**。
今回のみ、下の「📥 手動でダウンロード」またはこのページから .rbz を取得し、
SketchUp「拡張機能マネージャ → 拡張機能をインストール」で上書きしてください。
**v0.3.19 以降はボタンだけで更新が完結します。**

### ①【重要】確認ダイアログ付きのボタンが実機で無反応だった問題を修正

SketchUp のダイアログ環境では、ブラウザ標準の確認ダイアログ（confirm）が表示されず、
**常にキャンセル扱い**になることが分かりました。このため「⬇ ダウンロードして更新」をはじめ、
機種DBの削除・出荷時リセット・タブ設定の初期化・プリセット削除・キャッシュ削除・共有申請・
影パネル削除・ライセンス申請など、**確認を挟む18個のボタンが「押しても何も起きない」状態**でした。
すべて画面内の確認ダイアログに置き換え、正常に動作するようにしました。

### ② 更新の進捗・結果が見えるようになりました

オンライン更新の全工程（ダウンロード開始 → 受信KB進捗 → 検証 → インストール → 失敗理由）を
画面とライブログの双方に表示します。「実行されたのか分からない」状態を解消しました。
社内ネットワークのURLフィルタがダウンロードを遮っている場合は、それを検出して
**「🔓 社内フィルタの確認ページを開く」ボタン**を表示します（同意後に再実行すれば通ります）。

### ③ 🤖 デマンドの「読み方」をAIが判定（AI設定時のみ）

デマンドタブの「② 読み方の確認」に、**AIに読み方を判定してもらうボタン**を追加しました。
候補ごとの統計（年間kWh・ピークkW・日数・表の見出し）をもとに、需要データとして妥当な
シート・範囲・単位を根拠付きで推薦し、ワンクリックで画面に反映できます。
**取込の最終確定は従来どおり人が行います。コマごとの生データは送信しません。**

### ④ 社内フィルタの「同意ページ」まわりの案内を改善

同意した直後に真っ白なページが表示されることがありますが、これは遷移先がデータ用APIで
画面を持たないためで、**正常です**。その旨を画面に明記しました。ウィンドウを閉じて
「🔌 接続テスト」を再実行してください。

### 導入

**今回のみ手動**: 本rbzをSketchUp拡張機能マネージャで上書きインストール→SketchUp再起動。
次回（v0.3.20〜）からはプラグイン内「⬆更新」ボタンで完結します。
