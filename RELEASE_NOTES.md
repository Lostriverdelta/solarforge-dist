## SolarForge v0.3.23

### 🧭 特殊書式のデマンドデータをAIが「設計」して読めるようになりました

転置（日が列方向に並ぶ）・倍率（CT比）列・変則配置など、**自動検出が候補を出せない書式**でも、
AIが読み方（抽出仕様: シート・行列範囲・転置・日付列・単位・倍率列）を設計し、
**数値そのものは必ずプラグイン本体がファイルから読み取ります**（AIが数値を書くことはありません）。

- 候補が出ない場合は自動で起動。手動起動は「🧭 特殊書式をAIに設計させる」
- AIへ送るのは**見出しと列ごとの統計（min/max/mean）のみ** — コマ値の系列は送りません
- 設計内容と根拠を確認して「▶ この設計で取り込む」で確定（最終確定は従来どおり人）
- 取込後は通常どおり欠落補完・ヒートマップ・PV突合が動きます

### 導入

プラグイン内「⬆更新」ボタンから自動更新できます。
※本機能はAIバックエンドの更新も必要です（configには配備済み・追加設定不要）。
