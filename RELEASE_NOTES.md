## SolarForge v0.3.27

### 🌐 社内ネットワークで敷地取込・位置設定が使えるようになりました（重要）

直接続が遮断された（プロキシ必須の）社内ネットワークで、位置の自動解決・GSI標高／写真タイル・
PLATEAU取込が全滅していた問題を修正しました。

- 起動時に**Windowsのプロキシ設定を自動検出**し、Ruby・Pythonの両通信経路へ適用します
  （静的設定に加え、**PAC自動構成スクリプトのみの環境にも対応**）
- 自動検出できない場合は、メンテタブ →「🌐 通信診断」カードの**プロキシ欄**に
  アドレス（例: `proxy.example.co.jp:8080`）を入力すると**即適用**されます（再起動不要）
- 適用中のプロキシと検出元は同カードに常時表示。`none` で強制直接続（切り分け用）
- 通信診断の判定文も「refusedが並ぶ＝プロキシ必須ネットワーク」の案内に改善

### 確認方法

更新後にSketchUpを再起動し、メンテタブ → 「🌐 通信診断を実行」。
プロキシ欄の下に「適用中: …」が出て、各エンドポイントが ✓ になれば復旧です。
自動検出に失敗している場合はプロキシ欄に社内プロキシを手動入力→再診断してください。

### 導入

プラグイン内「⬆更新」ボタンから自動更新できます（v0.3.21以降）。
