## SolarForge v0.3.12

### ニュースが「タイムアウト」で表示されない問題を修正
制度・市場ニュースの**初回生成**（AIが公開情報を検索するため時間がかかります）が待ち時間を超えて表示されないことがありました。

- 待ち時間を延長し、それでも間に合わない場合は**60秒後に自動で再取得**します（その時点ではサーバ側で生成が完了しているため即座に表示されます）。
- サーバ側も生成が短時間で終わるよう調整しました。
- ニュースが出ない間もバナーが非表示になるだけで、**設計作業には影響しません**。

### 機種DBの重複登録を防止（内部改善）
機種データを追加・更新する際に、**同じ機種が型番の表記違いで二重登録される**のを防ぐ仕組みを入れました。

- 型番の大文字小文字・ハイフン・空白の違いを**同一とみなして照合**するようにし、プラグイン更新時のDB取り込みで同じ機種が増え続けるのを防ぎます。
- 機種データの矛盾（MPPT電圧範囲の上限が機器の最大DC入力電圧を超えている等）に加え、**表記違いの重複・型番が酷似する機種・主要諸元が完全に同じ別型番**も検出するようにしました。今後の国産PCS追加に備えた土台です。
- 現在収録の機種（パワコン61・パネル74・蓄電池46）に矛盾がないことを確認済みです。

### 導入
本rbzをSketchUp拡張機能マネージャで上書きインストール→SketchUp再起動、またはプラグイン内「⬆更新」から。
