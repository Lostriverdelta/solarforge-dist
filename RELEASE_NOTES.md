## SolarForge v0.3.10

### インバータ整合チェック：過積載率の誤表示を修正
インバータDBに**最大DC入力容量が未記載**の機種（Sungrow SG110CX／SG125HX-JP／SG320HX／SG350HX、Huawei SUN2000-4.95K系、オムロン KP55S3-HY-3A ほか計9機種）で、**過積載率（DC/AC比）と必要台数が誤って表示**されていました。

- **原因**：容量が未記載だと内部的に「1台構成」と仮定して計算していたため、例えば総DC500kWに対し100kW機を1台とみなして**過積載率5.0＝NG**と表示していました（必要台数も「-」）。
- **修正**：諸元が不足している機種は、**過積載率と必要台数を計算せず「諸元不足」と明示**するようにしました（マウスを乗せると理由が出ます）。**過電圧・MPPT電圧窓・最大直列枚数の判定は従来どおり**動作します（こちらは必要な諸元が揃っています）。
- 該当機種を使う場合は、DBタブで最大DC入力容量を入力いただくと、過積載率・必要台数も表示されます。

### DBの自動検査を追加（今後の機種追加の安全性向上）
機種を追加・編集した際に、**電気的に矛盾したデータを機械的に検出**する検査を追加しました。とくに「MPPT電圧範囲の上限が機器の最大DC入力電圧を超えている」ようなデータは、**過電圧の判定を誤らせる危険**があるため必ず検出します。現在収録の61機種は矛盾ゼロを確認済みです。

### 導入
本rbzをSketchUp拡張機能マネージャで上書きインストール→SketchUp再起動、またはプラグイン内「⬆更新」から。
