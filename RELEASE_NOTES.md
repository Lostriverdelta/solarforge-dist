## SolarForge v0.3.18

### 📊 新機能：デマンドタブ — 客先の需要データを取り込み、自家消費・余剰を実測ベースで算出

客先からもらう需要（デマンド）データを読み込み、発電シミュレーションと**時間ごとに**突き合わせて、
**消費可能電力量（自家消費）・余剰電力量・買電量・自家消費率・需要カバー率**を算出する専用タブを追加しました。
これまで「自家消費率◯％」と仮定していた部分を、実測ベースの数字に置き換えられます。

- **Excel（.xlsx / .xlsm）をそのまま読めます**（CSVへ保存し直す手間は不要）。CSV・TSVも対応
- **24／48／96コマ**、**kW／kWh** のどちらでも取込可能
- **月別シート（1月〜12月）に分かれたブックは自動判別し、まとめて結合**。日付が「1日」形式でもシート名から月を補完
- 1シートに複数の表が縦積みの場合（消費電力／発電量／余剰…）は自動分割し、見出し付きで候補表示
- **データが欠けている月は月名で名指しして警告**（欠損は0扱いで年間値が過小になる旨も明示）
- 書式の解釈は自動で確定せず、候補ごとの**年間kWh・ピークkW・日数**を見て人が選ぶ方式（誤読を数字で見抜けます）
- **BESSタブは取り込んだ実デマンドを自動的に優先**（従来の「負荷 kW平均」は取込が無い時だけ使用）。
  どちらの負荷で計算したかは結果に必ず表示します。取込値はSKPモデルに保存され、再起動後も有効

### 🔆 反射光（グレア）解析を専用タブへ分離

シミュレーションタブに埋もれていた反射光解析を独立タブにしました。
半球スキャン・3D反射光線の描画・受光点ごとの精密判定を1つのタブで操作できます（機能は従来どおり）。

### 📄 見積Excel連携 — レポートHTML 1ファイルから見積ブックへ取込

設計レポートHTMLの末尾に「Excel取込データ」節を追加しました（既定は非表示・ボタンで表示）。
諸元8項目（型式／枚数／出力W／DC容量／年間発電量／比発電量／影ロス率／重量）と、
**365日×24時間の時別発電量（素の数値）**を機械可読な表として同梱します。

- 見積ブック用の取込マクロ（`tools/excel/SolarForgeImport.bas`）をソースリポジトリに用意。
  レポートHTMLを選ぶだけで helio シート（365日×24時間）・見積の諸元欄・試算のDC容量を自動更新
- マクロを使わない場合も「📋 TSVコピー」ボタンで直接貼り付け可能

### 🔓 社内ネットワークの「未分類URL同意」ページに対応

社内のURLフィルタでAI解説・ニュース・機種DB共有が HTTP 403 になる場合、それが
「同意すれば通る確認ページ」なら、**プラグイン内で確認ページを開くボタン**を表示するようにしました
（同意はプラグインと同じブラウザで行う必要があるため）。恒久的な遮断の場合は、
情報システム部門へ依頼すべき内容（対象ドメイン名）を具体的に表示します。

### その他の改善

- 傾斜架台「選択面に敷き詰め」のヒント文を実装に合わせて修正
- PCS DBカードの機種数をDBの実件数から表示（固定値「35機種」の記載ズレを解消）
- エラーメッセージの文面整理（方言の削除・次の操作の明示）
- 取扱説明書を全面整備（全17章＋付録・印刷用HTML。ソースリポジトリ `docs/` に同梱）

### 導入

本rbzをSketchUp拡張機能マネージャで上書きインストール→SketchUp再起動、またはプラグイン内「⬆更新」から自動更新。
