## SolarForge v0.3.15

### パワコン（PCS）の選び方を3つの機能で拡張しました

#### ① 自立機能つきの小型PCSを1台だけ混ぜる条件設定（地域活用要件）
50kW未満の**余剰売電**では、地域活用要件（自立運転機能）を満たすために4.95kW級の自立機能つきPCSを1台だけ投入することがあります。ストリングタブの「🏠 補助として自立機能PCSを1台含める」から設定できます。

- **これまでの選定ロジックはそのままです。** 補助PCSが受け持つぶんのパネルを先に取り分け、**残りの容量で主PCSの台数を従来どおり**決めます。候補一覧の並び方・計算式は変わりません。
- 取り分けるストリングの直列枚数は、**補助PCS自身のMPPT電圧範囲・低温Voc・DC入力上限**から決めます。主PCS（例：1000V入力の産業用）と補助PCS（例：600V入力の住宅用）で電圧範囲が違っても、どのストリングも接続先のパワコンの範囲内に収まります。
- 取り分けができない場合（面の枚数が足りない／DC上限に最小直列が入らない）は、**理由を明示してエラー**にします。要件を満たしていない構成が「満たしたように」通ってしまわないようにするためです。
- ※主PCS自体が自立機能を持つ機種（安川電機25kW など）の場合は、この補助PCSは不要です。その場合は画面上でもその旨をお知らせします。

#### ② PCS構成を自由に選定できるオプション（出力問わず）
「🧩 PCS構成を自由に選定する」で、**機種と台数を自分で指定**して設計できます（候補検索や過積載率からの自動台数決定を使いません）。

- 最大AC容量の機種を主PCSとして扱い、それ以外の機種は①と同じ仕組みで取り分けます。
- **台数は指定どおり**です。MPPTの回路数が足りない場合も勝手に台数を増やさず、「あと何台必要か」を警告でお知らせします。
- 合計台数・AC容量・過積載率・主PCSがどれになるかを、追加した時点で画面に表示します。

#### ③ 候補に出てくるPCSを限定する機能
「🎯 候補PCSを限定する」で、**メーカー・AC出力の範囲・対象型番のみ・除外型番・自立機能つきのみ**を指定できます。社内で採用している機種だけを候補に出したいときにお使いください。

- 空欄の条件は「制限なし」です。**何も指定しなければ従来とまったく同じ結果**になります。
- 型番の照合は表記のゆれ（ハイフン・空白・大文字小文字）を無視するので、「KPV-A55-J4」と「kpv a55 j4」のどちらで書いても効きます。
- 絞り込み中は候補一覧の上に「○/○機種を対象」と表示され、条件を忘れて「候補が出ない」と悩まずに済みます。

### 機種DBに「自立」欄を追加しました
DBタブのパワコン一覧に**自立**列（自立運転機能）が付きました。**未登録 / 有 / 無**の3状態です。

- 出荷時は**全機種「未登録」**です。自立機能の有無は地域活用要件の根拠になるため、**メーカー仕様書で確認できたものだけ**を登録してください。当社側で推測して「有」と入れることはしていません。
- 「未登録」を「無し」と決めつけることもしません。画面では「—（未確認）」と表示し、補助PCSに未登録の機種を選んだ場合は確認をお願いする警告を出します。
- 登録した内容は**機種DBの社内共有（v0.3.13）でそのまま共有**できます。一度確認して登録すれば社内全員に配れます。

### あわせて直したこと（複数機種構成での整合）
複数機種の構成にしたとき、これまで主PCSだけを見ていて誤差が出ていた箇所を修正しました。

- **発電シミュレーションのピークカット**：AC容量の上限を**構成全体の合計AC**で判定するようにしました。従来は主PCSぶんだけで判定していたため、補助PCSのAC容量が抜け落ち、**年間発電量を過小評価**していました。
- **手動割付の過電圧チェック**：構成の中で**もっとも厳しい電圧上限**で判定します（入力電圧の低い住宅用PCS側の過電圧を見逃さないため）。
- **ストリングの移動**：MPPT本数の上限を**移動先のパワコンの機種**で判定します。
- **レポート**：PCS構成の内訳表（機種・台数・AC定格・自立機能）とAC合計を出力します。

### 導入
本rbzをSketchUp拡張機能マネージャで上書きインストール→SketchUp再起動、またはプラグイン内「⬆更新」から自動更新。
