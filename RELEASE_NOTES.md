## SolarForge v0.3.25

### 🔓 社内フィルタの自動同意が「実際に効く」ようになりました（重要）

v0.3.21〜24の自動同意（裏側での通信による同意）は、ブラウザのCookie保護
（SameSite）により**実環境では同意が成立していませんでした**。

本バージョンから、同意が必要になると**小さな同意ウィンドウが数秒だけ自動で開き、
自動で閉じます**（操作は不要です）。この方式はページを実際に表示するため確実に
同意が成立し、閉じた後は止まっていた処理（AI判定・ニュース・機種DB共有・更新DL）が
自動で再実行されます。

- 初回のみ「以後自動で同意するか」の確認が1回出ます（以降は全自動）
- フィルタが同意を定期的に再提示してきても、そのつど自動で処理されます
- 自動同意ON中は重複していた手動ボタン表示を整理しました
- ニュース取得スキップのログ連投も抑制しました

### 導入

プラグイン内「⬆更新」ボタンから自動更新できます（v0.3.21以降）。
更新後にSketchUpを再起動し、起動時に小さなウィンドウが数秒開閉した後
ニュースが表示されれば正常です。
