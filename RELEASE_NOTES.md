## SolarForge v0.3.16

### v0.3.15（PCS選定の3機能）を徹底検証し、見つかった不具合8件を修正しました

前バージョンで追加した「補助自立PCS／PCS構成の自由選定／候補の限定」を含む計算ロジックと使い勝手を多角的に検証し、確認できた不具合をすべて修正しました。とくに**セキュリティ上重要な1件**と、**電気設計の判定に関わる複数件**を含みます。

#### 【重要・セキュリティ】機種DB画面の表示で不正な文字列が実行され得る問題を修正
機種DB（パネル／パワコン／蓄電池）の一覧表で、型番・メーカー名などの文字列を画面へ表示する際に無害化（エスケープ）が漏れていました。**社内共有DBから取り込んだ他の担当者の入力**に細工が含まれると、DB画面を開かなくても起動時に不正なスクリプトが動く可能性がありました。表示箇所をすべて無害化しました。**社内でDB共有機能をお使いの環境は本バージョンへの更新を推奨します。**

#### 補助自立PCS（地域活用要件）を複数台にしたときの取り分けを修正
補助PCSの台数を2台以上に指定したとき、1本のストリングが**1台のパワコンのDC入力上限を超える**直列数になり、かつ2台目が空になることがありました。1本＝1台という物理制約に沿って、直列数を1台あたりの上限で決めるよう修正しました（例：4.95kW×2台なら10直×2本＝両台に正しく配分）。

#### 自由構成でパワコンのDC入力上限を超えたら警告するように
「PCS構成を自由に選定」で台数を指定したとき、過積載率の上限だけを緩めると、機種のDC入力上限を超えた構成が無警告で通っていました。上限超過を明示的に警告します。

#### 自立機能「無」と登録済みのパワコンを補助PCSに選んでも無警告だった問題を修正
自立機能が「無」と確定登録されている機種を補助自立PCSに指定しても警告が出ず、地域活用要件を満たさない設計が黙って通っていました。警告を追加し、レポートの自立機能欄も**有／無／—（未登録）の3状態**で表示するようにしました。

#### インバータ整合チェックのMPPT電圧範囲を温度補正するように（既存の不具合）
設計エンジンは動作電圧（Vmp）を温度補正して判定するのに、出力タブの「インバータ整合チェック」はSTC（25℃）のままで判定しており、**夏の高温でMPPT下限割れ・冬の低温で上限超過**する構成を「合格」と誤表示していました。設計エンジンと同じ式（高温Vmp≥下限・低温Vmp≤上限）に揃えました。

#### ストリング直列数の選定で「均等割り」が正しく最優先されるように（既存の不具合）
方位グループのストリング構成で、業界標準の**均等割り（全ストリング同じ直列数）**が選べる場合でも、まれに直列数の混在した劣後案が選ばれることがありました。均等割り案を必ず候補に含めて最優先するよう修正しました（本数が最少になる点は従来どおり）。

#### 手動割付ツールの過電圧チェックが寒冷地で甘かった問題を修正（既存の不具合）
手動でストリングを割り付ける際の過電圧警告が、設計最低気温を−10℃固定で判定しており、**寒冷地（例：帯広−25℃）で実際の過電圧を見逃す**おそれがありました。設計時に使った地域の最低気温を引き継いで判定し、使用中の気温をステータスにも表示します。

### 導入
本rbzをSketchUp拡張機能マネージャで上書きインストール→SketchUp再起動、またはプラグイン内「⬆更新」から自動更新。
